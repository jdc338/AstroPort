<div class="containerrr">
  <div class="view-page">
    <div class="card-view">
      <h2><%= @spaceship.name %></h2>
      <p></p>
      <p class="view-content">Description: <%=@spaceship.description %></p>
      <p></p>
      <p class="view-content">Armament: <%= @spaceship.armaments %></p>
      <p></p>
      <p class="view-content">Speed: <%= @spaceship.speed %> lightyears/second</p>
      <p></p>
      <p class="view-content">Price: ⓩ<%= @spaceship.price %></p>
      <p></p>
      <p class="view-content">Capacity: <%= @spaceship.capacity %><p>
    </div>
    <div class="view-img"><%=cl_image_tag @spaceship.photo.key, height: 600, width: 650%></div>
  </div>
  <br>
    <%= link_to "Make a booking", new_spaceship_booking_path(@spaceship, @booking), class: "btn btn-primary" %>
    <% if @spaceship.user == current_user %>
      <%= link_to "Edit Details", edit_spaceship_path(@spaceship), class: "btn btn-primary" %>
      <%= link_to "Remove My Spaceship", spaceship_path(@spaceship), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-primary" %>
    <% end %>
</div>
<%# <ul class="list-unstyled"> %>
  <%# <% @spaceship.reviews.each do |review| %>
  <%# <% if booking.review.exist?%>
    <%# <li><%= review.rating %>
    <%# <%= review.content %>
    <%# </li> %>
<%# </ul> %>
